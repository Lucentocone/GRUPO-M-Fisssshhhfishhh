services:
  app:
    build: .
    container_name: aplicacion
    depends_on:
      - db

  db:
    image: postgres:16
    container_name: base_datos
    environment:
      POSTGRES_DB: proyecto
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123